server:
  port: 8082
spring:
  application:
    name: notification
  datasource:
    url: jdbc:postgresql://localhost:5232/notification
    username: antares
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true